{
  "name": "stokku",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install && npm run setup:backend && npm run setup:frontend",
    "setup:backend": "cd backend && cp .env.example .env && composer install && php artisan key:generate && php artisan jwt:secret",
    "setup:frontend": "cd frontend && cp .env.example .env && npm install",
    "all": "concurrently -k -n \"BACKEND\",\"FRONTEND\" -c \"yellow,cyan\" \"cd backend && php artisan serve\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
